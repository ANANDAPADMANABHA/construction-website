# Arcfinity — robots.txt
# Allow all crawlers full access to public content.
User-agent: *
Allow: /

# Keep utility pages out of crawling (also noindex'd in-page).
Disallow: /thank-you

# Content admin panel — not for search engines.
Disallow: /admin

# Sitemap (generated by @astrojs/sitemap at build).
# NOTE: update the domain if it changes; must match astro.config.mjs `site`.
Sitemap: https://www.arcfinity.in/sitemap-index.xml
